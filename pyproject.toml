[project]
name = "ai-radar"
version = "0.1.0"
description = "AI Radar - AI Intelligence Filtering System"
requires-python = ">=3.11"

[tool.uv]
dev-dependencies = [
    "pytest>=7.4",
    "pytest-asyncio>=0.23",
    "ruff>=0.1",
]
